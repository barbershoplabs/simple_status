<!DOCTYPE html>
<html>
<head>
  <%= render '/common/head' %>
</head>
<body>
  <div class="navbar">
    <div class="navbar-inner">
      <div class="container">
        <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target="#nav-collapse-01"></button>

        <%= link_to "Simple Status", customer_root_url, class: "brand" %>
        <div class="nav-collapse collapse" id="nav-collapse-01">
          <ul class="nav">
            <li class= <%= 'active' if params[:controller] == 'admin/home' %>><%= link_to "Admin Home", admin_customer_root_path %></li>
            <li class= <%= 'active' if params[:controller] == 'admin/teams' %>><%= link_to "Team Management", admin_teams_path %></li>
            <li class= <%= 'active' if params[:controller] == 'admin/status_reports' %>><%= link_to "Status Reports Management", admin_status_reports_path %></li>
          </ul> <!-- /nav -->
          <ul class="nav pull-right">
            <% if current_user %>
              <li><%= link_to "Signed in as: #{current_user.email}", "#" %></li>
              <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li><%= link_to 'Sign In', new_user_session_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <%= render '/common/notification' %>

  <div class="container">
    <div class="row">
      <%= yield %>
    </div>
  </div>


  <%= render '/common/analytics' %>
</body>
</html>
